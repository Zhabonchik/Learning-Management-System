'use strict';

const pathRewriter = require('@sap/approuter/lib/middleware/path-rewriter');

module.exports = function pathRewrite(req, res, next) {
  req.internalUrl = pathRewriter(req, true);
  next();
};
